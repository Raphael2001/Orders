# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
"on": pull_request
jobs:
  build_and_preview:
    if: "${{ github.event.pull_request.head.repo.full_name == github.repository }}"
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    env:
      ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: true
    steps:
      - uses: actions/checkout@v6
      - uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: 'projects/549327547136/locations/global/workloadIdentityPools/github-pool/providers/github-provider'
          service_account: 'github-action-627878624@orders-1f4fb.iam.gserviceaccount.com'
      - name: Deploy PR preview to Firebase Hosting
        env:
          NODE_OPTIONS: --max-old-space-size=4096
        run: npx firebase-tools@latest hosting:channel:deploy "pr-${{ github.event.number }}" --project orders-1f4fb --expires 7d
