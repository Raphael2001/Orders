# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
"on":
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v6
      - uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: 'projects/549327547136/locations/global/workloadIdentityPools/github-pool/providers/github-provider'
          service_account: 'github-action-627878624@orders-1f4fb.iam.gserviceaccount.com'
      - uses: FirebaseExtended/action-hosting-deploy@v0
        env:
          NODE_OPTIONS: --max-old-space-size=4096
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          channelId: live
          projectId: orders-1f4fb
